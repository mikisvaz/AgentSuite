
user:

agent: Manager
introduce: SC26
tool: SC26
endpoint: glm

Use the SC26 to find out the TFs for each timepoint for each treatment and save
each of them in tmp/<treatment_name>.json. To avoid overpopulating the context
with tool cals, ask the Worker agent to process each treatment and save the
results separatedly in a new conversation. Call the Worker with ask once per treatment
